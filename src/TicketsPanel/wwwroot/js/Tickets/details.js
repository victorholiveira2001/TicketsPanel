function openReplyBox() {
    // Exibe a textarea e o botão de enviar quando "Responder" for clicado
    document.getElementById('reply-textarea').removeAttribute('hidden');
    document.getElementById('send').removeAttribute('hidden');
}

function openFinishModal(ticketId) {
    // Exibe a modal de finalização de mensagem.
}